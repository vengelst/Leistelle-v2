{
  "name": "@leitstelle/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/tests/**/*.test.js\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "dev": "npm run build"
  },
  "dependencies": {
    "@types/leaflet": "^1.9.21",
    "jspdf": "^4.2.1",
    "leaflet": "^1.9.4"
  }
}
